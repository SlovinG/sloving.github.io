import{j as s,b as o,c as t,aa as e}from"./chunks/framework.DDeudtUa.js";const n="data:image/png;base64,",c="/assets/640-1691661604690-2.KiuFw8DV.png",i="/assets/640-1691661604690-3.C3N-skY4.png",r="/assets/640-1691661604690-4.BcN83WEp.png",a="/assets/640-1691661604690-5.DcZFg9kS.gif",p="/assets/640-1691661604690-6.DWkNQGDI.png",g="/assets/123123123133.BgaXLFoL.png",k="/assets/640-1691736892294-92.B99ulq7J.png",d="/assets/123123123123.DYKfZHP8.png",l="/assets/image-20230815141859256.Dx5TK_sc.png",h="/assets/image-20230815141932512.DpyTHUWV.png",u="/assets/image-20230815142005762.BGuN0Q_p.png",m="/assets/640-1691661604690-7.Cjdm3Jas.png",E="/assets/640-1691661604691-8.DoD4JWrj.png",A="/assets/image-20230815142056102.2J53Spac.png",S="/assets/640-1691661604691-9.CyyrIbAh.png",_="/assets/640-1691661604691-10.CmOMUtEE.png",q="/assets/640-1691661604691-11.BFVGB4MO.png",f="/assets/640-1691661604691-12.CgJ6euks.png",y="/assets/640-1691662542368-62.e-n-QXLb.png",b="/assets/640-1691661604691-14.kpdti_sJ.png",x="/assets/640-1691661604691-15.C5PG_YO3.png",Y=JSON.parse('{"title":"一文详解Socket","description":"","frontmatter":{"title":"一文详解Socket","date":"2023-08-10T00:00:00.000Z","tags":["Socket"],"categories":["计算机网络"]},"headers":[],"relativePath":"ComputerNetworks/一文详解Socket.md","filePath":"ComputerNetworks/一文详解Socket.md","lastUpdated":null}'),F={name:"ComputerNetworks/一文详解Socket.md"},P=e('<h1 id="一文详解socket" tabindex="-1">一文详解Socket <a class="header-anchor" href="#一文详解socket" aria-label="Permalink to &quot;一文详解Socket&quot;">​</a></h1><h2 id="socket-概念" tabindex="-1">Socket 概念 <a class="header-anchor" href="#socket-概念" aria-label="Permalink to &quot;Socket 概念&quot;">​</a></h2><p>故事要从一个 <strong>插头</strong> 说起。</p><p><img src="'+n+'" alt="图片"></p><p>当我将 <strong>插头</strong> 插入 <strong>插座</strong>，那看起来就像是将两者连起来了。</p><p><img src="'+c+'" alt="图片"></p><p>而插座的英文，又叫 <code>Socket</code>。</p><p>巧了，我们搞网络编程时也会用到一个叫 <code>Socket</code> 的东西。</p><p>其实两者非常相似。通过 <code>Socket</code>，我们可以与某台机子建立&quot;<strong>连接</strong>&quot;，建立&quot;连接&quot;的过程，就像是将插口插入插槽一样。</p><p><img src="'+i+'" alt="图片"></p><p>到这里其实我们对 <code>Socket</code> 还是很模糊。</p><p>从我们最熟悉的 <strong>使用场景</strong> 开始说起。</p><h2 id="socket-使用场景" tabindex="-1">Socket 使用场景 <a class="header-anchor" href="#socket-使用场景" aria-label="Permalink to &quot;Socket 使用场景&quot;">​</a></h2><p>我们想要将数据从 A 电脑的某个进程发送到 B 电脑的某个进程里。</p><p>这时候我们需要选择将数据发过去的 <strong>方式</strong>，如果需要确保数据能完整发给对方，那就选可靠的 <code>TCP</code> 协议，如果数据丢了也没关系，那就选择不可靠的 <code>UDP</code> 协议。</p><p>初学者毫无疑问，首选 <code>TCP</code>。</p><p><img src="'+r+'" alt="图片"></p><p>那这时候就需要用 <code>Socket</code> 进行编程。</p><p>于是第一步就是创建一个关于 TCP 的 <code>Socket</code>，就像下面这样。</p><div class="language-java vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">java</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">sock_fd </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">=</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;"> Socket</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(AF_INET, SOCK_STREAM, IPPROTO_TCP);</span></span></code></pre></div><p>这个方法会返回 <code>Socket_fd</code>，它是 Socket 文件的 <strong>句柄</strong>（用来标识对象或者项目的标识符），是个数字，相当于Socket的身份证号。</p><p>得到了 <code>Socket_fd</code> 之后，对于服务端，就可以依次执行 <code>bind()</code>, <code>listen()</code>, <code>accept()</code> 方法，然后坐等客户端的连接请求。</p><p>对于客户端，得到 <code>Socket_fd</code> 之后，你就可以执行 <code>connect()</code> 方法向服务端发起建立连接的请求，此时就会发生 TCP 三次握手。</p><p><img src="'+a+'" alt="图片"></p><p>连接建立完成后，<strong>客户端</strong> 可以执行 <code>send()</code> 方法发送消息，<strong>服务端</strong>可以执行 <code>recv()</code> 方法接收消息，反过来，<strong>服务器</strong> 也可以执行<code>send()</code>，<strong>客户端</strong> 执行 <code>recv()</code> 方法。</p><p>到这里为止，就是我们大部分程序员最熟悉的使用场景。</p><h2 id="socket-设计" tabindex="-1">Socket 设计 <a class="header-anchor" href="#socket-设计" aria-label="Permalink to &quot;Socket 设计&quot;">​</a></h2><p>现在，Socket 我们见过，也用过，但对大部分程序员来说，它是个 <strong>黑盒</strong>。</p><p>那既然是黑盒，我们索性假设我们忘了 Socket，重新设计一个内核网络传输功能。</p><p>网络传输，从操作上来看，无非就是，发数据和远端之间 <strong>互相</strong> 收发数据，也就是对应着 <strong>写数据</strong> 和 <strong>读数据</strong>。</p><p><img src="'+p+'" alt="图片"></p><p>但显然，事情没那么简单，这里还有两个问题：</p><ol><li>接收端和发送端可能不止一个，因此我们需要一些信息做下区分，这个大家肯定很熟悉，可以用 IP 和端口。<strong>IP 用来定位是哪台电脑，端口用来定位是这台电脑上的哪个进程。</strong></li><li>发送端和接收端的传输方式有很多区别，可以是可靠的 <code>TCP协议</code>，也可以是不可靠的 <code>UDP协议</code>，甚至还需要支持基于 <code>icmp协议</code> 的<code>ping命令</code>。</li></ol><h3 id="sock-是什么" tabindex="-1">sock 是什么 <a class="header-anchor" href="#sock-是什么" aria-label="Permalink to &quot;sock 是什么&quot;">​</a></h3><p>写过代码的都知道，为了支持这些功能，我们需要定义一个 <strong>数据结构</strong> 去支持这些功能。</p><p>这个数据结构，叫 <code>sock</code>。</p><p>为了解决上面的第一个问题，我们可以在 <code>sock</code> 里加入 <strong>IP和端口</strong> 字段。</p><img src="'+g+'" alt="图片" style="zoom:67%;"><p>而第二个问题，我们会发现这些协议虽然各不相同，但还是有一些功能相似的地方，比如收发数据时的一些逻辑完全可以复用。按面向对象编程的思想，我们可以将不同的协议当成是不同的 <strong>对象类（或结构体）</strong>，将公共的部分提取出来，通过&quot;<strong>继承</strong>&quot;的方式，复用功能。</p><h3 id="基于各种-sock-实现网络传输功能" tabindex="-1">基于各种 sock 实现网络传输功能 <a class="header-anchor" href="#基于各种-sock-实现网络传输功能" aria-label="Permalink to &quot;基于各种 sock 实现网络传输功能&quot;">​</a></h3><p>于是，我们将功能 <strong>重新划分</strong> 下，定义了一些数据结构。</p><img src="'+k+'" alt="图片" style="zoom:80%;"><p><code>sock</code> 是 <strong>最基础</strong> 的结构，维护一些任何协议都有可能会用到的收发数据缓冲区。</p><p><code>inet_sock</code> 特指用了 <strong>网络传输</strong> 功能的 <code>sock</code>，在 <code>sock</code> 的基础上还加入了 <code>TTL</code>，<strong>端口，IP地址</strong> 这些跟网络传输相关的字段信息。说到这里大家就懵了，难道还有不是用网络传输的？有，比如 <code>Unix domain Socket</code>，用于本机进程之间的通信，直接读写文件，不需要经过网络协议栈。这是个非常有用的东西，我以后一定讲讲（画饼）。</p><p><code>inet_connection_sock</code> 是指 <strong>面向连接</strong> 的 <code>sock</code>，在 <code>inet_sock</code> 的基础上加入面向连接的协议里相关字段，比如 <code>accept队列</code>，数据包分片大小，握手失败重试次数等。虽然我们现在提到面向连接的协议就是指 TCP，但设计上 Linux 需要支持扩展 <strong>其他</strong> 面向连接的 <strong>新协议</strong>，</p><p><code>tcp_sock</code> 就是正儿八经的 <strong>tcp协议</strong> 专用的 <code>sock</code> 结构了，在 <code>inet_connection_sock</code> 基础上还加入了 TCP 特有的 <strong>滑动窗口</strong>、<strong>拥塞避免</strong> 等功能。同样 UDP 协议也会有一个专用的数据结构，叫 <code>udp_sock</code>。</p><p>好了，现在有了这套数据结构，我们将它们跟 <strong>硬件</strong> 网卡对接一下，就实现了网络传输的功能。</p><h3 id="提供-socket-层" tabindex="-1">提供 Socket 层 <a class="header-anchor" href="#提供-socket-层" aria-label="Permalink to &quot;提供 Socket 层&quot;">​</a></h3><p>可以想象得到，这里面的代码肯定非常复杂，同时还操作了网卡硬件，需要比较高的 <strong>操作系统权限</strong>，再考虑到性能和安全，于是决定将它放在 <strong>操作系统内核</strong> 里。</p><p>既然网络传输功能做在内核里，那用户空间的应用程序想要用这部分功能的话，该怎么办呢？</p><p>这个好办，本着不重复造轮子的原则，我们将这部分功能抽象成一个个 <strong>简单的接口</strong>。以后别人只需要调用这些接口，就可以驱动我们写好的这一大堆复杂的数据结构去发送数据。</p><p>那么问题来了，<strong>怎么样将这部分功能暴露出去呢？让其他程序员更方便的使用呢？</strong></p><p>既然跟远端服务端进程收发数据可以抽象为“<strong>读和写</strong>”，操作文件也可以抽象为&quot;<strong>读和写</strong>&quot;，正好有句话叫，&quot;<strong>linux里一切皆是文件</strong>&quot;，那我们索性，<strong>将内核的 sock 封装成文件</strong>就好了。创建 <code>sock</code> 的同时也创建一个<strong>文件</strong>，<strong>文件有个句柄fd</strong>，说白了就是个 <strong>文件系统</strong> 里的 <strong>身份证号码</strong>，通过它可以 <strong>唯一确定</strong> 是哪个<code>sock</code>。</p><blockquote><p>这个文件句柄fd其实就是 <code>sock_fd = Socket(AF_INET, SOCK_STREAM, IPPROTO_TCP)</code> 里的 <code>sock_fd</code></p></blockquote><p>将句柄暴露给用户，之后用户就可以像操作 <strong>文件句柄</strong> 那样去操作这个 <strong>sock句柄</strong>。在用户空间里操作这个句柄，<strong>文件系统</strong> 就会将操作 <strong>指向</strong> 内核<code>sock</code>结构。</p><p>是的，操作这个特殊的 <strong>文件</strong> 就相当于操作内核里对应的 <code>sock</code>。</p><img src="'+d+'" alt="图片" style="zoom:67%;"><p>有了 <code>sock_fd句柄</code> 之后，我们就需要提供一些接口方法，让用户更方便的实现特定的网络编程功能。这些接口，我们列了一下，发现需要有 <code>send()</code>，<code>recv()</code>，<code>bind()</code>, <code>listen()</code>，<code>connect()</code> 这些。到这里，我们的内核网络传输功能就算设计完成了。</p><p>现在是不是眼熟了，<strong>上面这些接口方法其实就是 Socket 提供出来的接口</strong>。</p><p>所以说，Socket 其实就是个 <strong>代码库</strong> or <strong>接口层</strong>，它介于 <strong>内核和应用程序之间</strong>，提供了一些 <strong>高度封装过</strong> 的接口，让我们去使用 <strong>内核网络传输功能</strong>。</p><img src="'+l+'" alt="image-20230815141859256" style="zoom:67%;"><p>到这里，我们应该明白了。我们平时写的应用程序里代码里虽然用了 Socket 实现了收发数据包的功能，但其实真正执行网络通信功能的，不是应用程序，而是 <strong>Linux 内核</strong>。相当于应用程序通过 Socket 提供的接口，将网络传输的这部分工作 <strong>外包</strong> 给了 <strong>Linux内核</strong>。</p><p>这听起来像不像我们最熟悉的 <strong>前后端分离</strong> 的服务架构，<strong>虽然这么说不太严谨</strong>，但看上去 Linux 就像是被分成了 <strong>应用程序和内核两个服务</strong>。内核就像是 <strong>后端</strong>，暴露了好多个 <strong>api接口</strong>，其中一类就是 Socket 的 <code>send()</code> 和 <code>recv()</code> 这些方法。应用程序就像是 <strong>前端</strong>，负责调用内核提供的接口来实现想要的功能。</p><img src="'+h+'" alt="image-20230815141932512" style="zoom:50%;"><p>看到这里，我担心大家会有点混乱，来做个小的<strong>总结</strong>。</p><p><strong>在操作系统内核空间里，实现网络传输功能的结构是 sock，基于不同的协议和应用场景，会被泛化为各种类型的 xx_sock，它们结合硬件，共同实现了网络传输功能。为了将这部分功能暴露给用户空间的应用程序使用，于是引入了 Socket 层，同时将 sock 嵌入到文件系统的框架里，sock 就变成了一个特殊的文件，用户就可以在用户空间使用文件句柄，也就是 Socket_fd 来操作内核 sock 的网络传输能力。</strong></p><p>这个 <code>Socket_fd</code> 是一个 <strong>int类型的数字</strong>。现在回去看 <code>Socket</code> 的中文翻译，<strong>套接字</strong>，<strong>我</strong> 将它理解为一 <strong>套</strong> 用于连 <strong>接</strong> 的数 <strong>字</strong>，是不是就觉得特别合理了。</p><img src="'+u+'" alt="image-20230815142005762" style="zoom:50%;"><h2 id="socket-如何实现网络通信" tabindex="-1">Socket 如何实现网络通信 <a class="header-anchor" href="#socket-如何实现网络通信" aria-label="Permalink to &quot;Socket 如何实现网络通信&quot;">​</a></h2><p>上面关于怎么实现网络通信功能这一块一笔带过了，现在我们来聊聊。</p><p>这套 sock 的结构其实非常复杂。我们以最常用的 TCP 协议为例，简单了解下它是怎么实现网络传输功能的。</p><p>我将它分为两阶段，分别是 <strong>建立连接</strong> 和 <strong>数据传输</strong>。</p><h3 id="建立连接" tabindex="-1">建立连接 <a class="header-anchor" href="#建立连接" aria-label="Permalink to &quot;建立连接&quot;">​</a></h3><p>对于 TCP，要传数据，就得先在客户端和服务端中间 <strong>建立连接</strong>。</p><p>在客户端，代码执行 Socket 提供的 <code>connect(sockfd, &quot;ip:port&quot;)</code> 方法时，会通过 <strong>sockfd 句柄</strong> 找到对应的 <strong>文件</strong>，再根据文件里的信息 <strong>指向</strong> 内核的 <code>sock</code> 结构。通过这个 sock 结构主动发起三次握手。</p><img src="'+m+'" alt="图片" style="zoom:80%;"><p>在服务端握手次数还没达到&quot;三次&quot;的连接，叫 <strong>半连接</strong>，完成好三次握手的连接，叫 <strong>全连接</strong>。它们分别会用 <strong>半连接队列</strong> 和 <strong>全连接队列</strong> 来存放，这两个队列会在你执行 <code>listen()</code>方法的时候创建好。当服务端执行 <code>accept()</code>方法时，就会从全连接队列里拿出一条全连接。</p><img src="'+E+'" alt="图片" style="zoom:80%;"><p>至此，连接就算准备好了，之后，就可以<strong>开始传输数据</strong>。</p><blockquote><p>虽然都叫队列，但半连接队列其实是个 hash 表，而全连接队列其实是个链表。</p><p>那么问题来了，为什么半连接队列要设计成哈希表而全连接队列是个链表？详见此文：<a href="https://mp.weixin.qq.com/s?__biz=Mzg5NDY2MDk4Mw==&amp;mid=2247487062&amp;idx=1&amp;sn=fa75a39e60f13e2275ff9ef7c96866de&amp;scene=21#wechat_redirect" target="_blank" rel="noreferrer">《没有accept，能建立TCP连接吗？》</a></p></blockquote><h3 id="数据传输" tabindex="-1">数据传输 <a class="header-anchor" href="#数据传输" aria-label="Permalink to &quot;数据传输&quot;">​</a></h3><p>为了实现发送和接收数据的功能，sock 结构体里带了 <strong>一个发送缓冲区和一个接收缓冲区</strong>，说是 <strong>缓冲区</strong>，但其实就是个 <strong>链表</strong>，上面挂着一个个准备要发送或接收的数据。</p><p>当应用执行 <code>send()</code> 方法 <strong>发送数据</strong> 时，同样也会通过 <code>sock_fd</code> 句柄找到对应的文件，根据文件指向的 <code>sock</code> 结构，找到这个 <code>sock</code> 结构里带的 <strong>发送缓冲区</strong>，将数据会放到发送缓冲区，然后结束流程，内核看心情决定什么时候将这份数据发送出去。</p><p><strong>接收数据</strong> 流程也类似，当数据送到 Linux 内核后，数据不是立马给到应用程序的，而是先放在接收缓冲区中，数据静静躺着，卑微的等待 <strong>应用程序</strong> 什么时候执行 <code>recv()</code> 方法来拿一下。</p><img src="'+A+'" alt="image-20230815142056102" style="zoom:50%;"><blockquote><p>IP和端口其实不在 sock 下，而在 inet_sock 下，上面这么画只是为了简化。。。</p></blockquote><p>那么问题来了，发送数据是应用程序主动发起，这个大家都没问题。</p><p><strong>那接收数据呢？数据从远端发过来了，怎么通知并给到应用程序呢？</strong></p><p>这就需要用到 <strong>等待队列</strong>。</p><img src="'+S+'" alt="图片" style="zoom:80%;"><p>当你的应用进程执行 <code>recv()</code> 方法尝试获取（阻塞场景下）接收缓冲区的数据时。</p><ul><li>如果有数据，那正好，取走就好了。这点没啥疑问。</li><li>但如果没数据，就会将自己的 <strong>进程</strong> 信息注册到这个 sock 用的 <strong>等待队列</strong> 里，然后进程 <strong>休眠</strong>。如果这时候有数据从远端发过来了，数据进入到接收缓冲区时，内核就会取出 sock 的等待队列里的进程，**唤醒 **进程来取数据。</li></ul><img src="'+_+'" alt="图片" style="zoom:80%;"><p>有时候，你会看到 <strong>多个进程</strong> 通过 <code>fork</code> 的方式，<code>listen</code> 了同一个 <code>Socket_fd</code>。在内核，它们都是 <strong>同一个sock</strong>，多个进程执行 <code>listen()</code> 之后，都嗷嗷等待连接进来，所以都会将自身的进程信息注册到这个 <code>Socket_fd</code> 对应的内核 sock 的<strong>等待队列</strong>中。如果这时真来了一个连接，是该唤醒等待队列里的哪个进程来接收连接呢？这个问题的答案比较有趣。</p><ul><li>在 Linux 2.6 以前，会唤醒等待队列里的所有进程。但最后其实只有一个进程会处理这个连接请求，其他进程又重新进入休眠，这些被唤醒了又无事可做最后只能重新回去休眠的进程会消耗一定的资源。就好像你在广东的街头，想问路，叫一声靓仔，几十个人同时回头，但你其实只需要其中一个靓仔告诉你路该怎么走。你这种一不小心 <strong>惊</strong> 动这 <strong>群</strong> 靓仔的场景，在计算机领域中，就叫 <strong>惊群效应</strong>。</li><li>在 Linux 2.6 之后，只会唤醒等待队列里的其中一个进程。是的，Socket 监听的惊群效应问题被修复了。</li></ul><p><img src="'+q+'" alt="图片"></p><p>看到这里，问题又来了。</p><p><strong>服务端 listen 的时候，那么多数据到一个 Socket 怎么区分多个客户端的？</strong></p><p>以 TCP 为例，服务端执行 listen 方法后，会等待客户端发送数据来。客户端发来的数据包上会有 <strong>源IP地址和端口</strong>，以及 <strong>目的IP地址和端口</strong>，这四个元素构成一个 <strong>四元组</strong>，可以用于唯一标记一个客户端。</p><blockquote><p>其实说四元组并不严谨，因为过程中还有很多其他信息，也可以说是五元组。。。但大概理解就好，就这样吧。。。</p></blockquote><p><img src="'+f+'" alt="图片"></p><p>服务端会创建一个新的内核sock，并用四元组生成一个<code>hash key</code>，将它放入到一个 <code>hash表</code> 中。</p><p><img src="'+y+'" alt="图片"></p><p>下次再有消息进来的时候，通过消息自带的四元组生成 <code>hash key</code> 再到这个 <code>hash表</code> 里重新取出 <strong>对应的sock</strong> 就好了。所以说 <strong>服务端是通过四元组来区分多个客户端的</strong>。</p><p><img src="'+b+`" alt="图片"></p><h2 id="sock-怎么实现-继承" tabindex="-1">sock 怎么实现&quot;继承&quot; <a class="header-anchor" href="#sock-怎么实现-继承" aria-label="Permalink to &quot;sock 怎么实现&quot;继承&quot;&quot;">​</a></h2><p>最后遗留一个问题。</p><p>大家都知道 Linux 内核是C语言实现的，而 <strong>C语言没有类也没有继承的特性，是怎么做到&quot;继承&quot;的效果的呢？</strong></p><p>在C语言里，结构体里的内存是 <strong>连续</strong> 的，将要继承的&quot;父类&quot;，放到结构体的 <strong>第一位</strong>，就像下面这样。</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tcp_sock {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* inet_connection_sock has to be the first member of tcp_sock */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inet_connection_sock inet_conn;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 其他字段</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inet_connection_sock {</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">    /* inet_sock has to be the first member! */</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> inet_sock   icsk_inet;</span></span>
<span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">        // 其他字段</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p>然后我们就可以通过结构体名的长度来强行截取内存，这样就能转换结构体，从而实现类似&quot;继承&quot;的效果。</p><div class="language-c vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">c</span><pre class="shiki shiki-themes github-light github-dark vp-code"><code><span class="line"><span style="--shiki-light:#6A737D;--shiki-dark:#6A737D;">// sock 转为 tcp_sock</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">static</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> inline</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tcp_sock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#6F42C1;--shiki-dark:#B392F0;">tcp_sk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">(</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">const</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;"> struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> sock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#E36209;--shiki-dark:#FFAB70;">sk</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">{</span></span>
<span class="line"><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">    return</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> (</span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">struct</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;"> tcp_sock </span><span style="--shiki-light:#D73A49;--shiki-dark:#F97583;">*</span><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">)sk;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">}</span></span></code></pre></div><p><img src="`+x+'" alt="图片"></p><h2 id="总结" tabindex="-1">总结 <a class="header-anchor" href="#总结" aria-label="Permalink to &quot;总结&quot;">​</a></h2><ul><li>Socket 中文套接字，我理解为一 <strong>套</strong> 用于连 <strong>接</strong> 的数 <strong>字</strong>。</li><li>sock 在内核，Socket_fd 在用户空间，Socket 层介于内核和用户空间之间。</li><li>在操作系统内核空间里，实现网络传输功能的结构是 sock，基于不同的协议和应用场景，会被泛化为各种类型的 xx_sock，它们结合硬件，共同实现了网络传输功能。为了将这部分功能暴露给用户空间的应用程序使用，于是引入了 Socket 层，同时将 sock 嵌入到文件系统的框架里，sock 就变成了一个特殊的文件，用户就可以在用户空间使用文件句柄，也就是 Socket_fd 来操作内核 sock 的网络传输能力。</li><li>服务端可以通过四元组来区分多个客户端。</li><li>内核通过 c 语言&quot;结构体里的内存是连续的&quot;这一特点实现了类似继承的效果。</li></ul>',115),C=[P];function v(D,U,B,T,I,X){return o(),t("div",null,C)}const Z=s(F,[["render",v]]);export{Y as __pageData,Z as default};
